# Falcon-Pad — Requirements
# pip install -r requirements.txt

fastapi>=0.100.0
uvicorn[standard]>=0.23.0
pydantic>=2.0.0
python-multipart>=0.0.6

# Optionnel — support .docx dans l'onglet Briefing
python-docx>=1.1.0

# Pour compiler en .exe (optionnel)
# pyinstaller>=6.0.0
